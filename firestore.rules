rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Each user can only read/write their own history
    match /users/{uid}/history/{entryId} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
